#!/usr/bin/env python3
"""Module that defines a type-annotated function returning the floor of a float."""


def floor(n: float) -> int:
    """Return the floor of the float n as an integer."""
    return int(n)
